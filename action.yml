name: 'check book image format'
description: 'check book image format'
author: 'srz-zumix'

inputs:
  path:
    description: |
      image directory
    default: .
  dpi:
    description: |
      require dpi
    default: 350
  pixel_limit:
    description: |
      maximum pixels
    default: 4000000
  formats:
    description: |
      require formats (comma sparate)
    default: 'JPEG'
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.path }}
    - ${{ inputs.dpi }}
    - ${{ inputs.pixel_limit }}
    - ${{ inputs.formats }}
branding:
  icon: 'plus-circle'
  color: 'blue'
